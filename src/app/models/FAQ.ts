export interface FAQ {
    question: string;   // La question fréquemment posée
    answer: string;     // La réponse à la question
    isOpen?: boolean;   // Indique si la réponse est ouverte ou fermée (optionnelle)
  }
  